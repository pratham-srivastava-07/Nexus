import * as React from 'react';
import { router, useLocalSearchParams } from 'expo-router';
import BountyDetailScreen from '../bounty/[id]';

export default function TaskDetailScreen() {
  return <BountyDetailScreen />;
}
